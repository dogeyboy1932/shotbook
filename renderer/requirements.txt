# Renderer venv (.venv-renderer) -- KEEP SEPARATE from the backend/audio venvs.
# torch>=2.4 here conflicts with the backend's pinned torch==2.1.0 (audiocraft).
#
# Mirrors _SPED/spedcopycausalforcing/requirements.txt (the engine's own deps),
# trimmed to the inference path, plus FastAPI for the service.
#
# flash-attn is installed SEPARATELY (needs nvcc + ninja, no build isolation):
#   pip install flash-attn --no-build-isolation
# See scripts/setup_renderer.sh.

# --- video engine (Wan2.1-1.3B / Causal-Forcing) ---
torch>=2.4.0
torchvision>=0.19.0
diffusers==0.31.0
transformers>=4.49.0
tokenizers>=0.20.3
accelerate>=1.1.1
numpy==1.24.4
omegaconf
einops
ftfy
sentencepiece
easydict
safetensors
opencv-python>=4.9.0.80
imageio
imageio-ffmpeg
ninja
huggingface_hub[cli]

# --- service + planning (Claude shot breakdown moved onto the VM) ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9          # file uploads for /ingest
pydantic>=2.0.0
pydantic-settings>=2.0.0
anthropic>=0.40.0

# --- ingestion-from-UI (.txt/.pdf -> Claude -> Supabase) ---
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
pypdf>=4.0.0
tqdm>=4.66.0
