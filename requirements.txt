# Core (all services)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
httpx>=0.27.0

# Mixer
websockets>=12.0

# SFX — install torch first with the correct CUDA index URL (see scripts/setup.sh)
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0
audiocraft
