# Core (all services)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
httpx>=0.27.0

# Mixer
websockets>=12.0

# SFX — pinned to match audiocraft 1.3.0's hard requirements
# Install via: pip install torch==2.1.0 torchaudio==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cu121
torch==2.1.0
torchaudio==2.1.0
torchvision==0.16.0
numpy>=1.24.0
audiocraft
